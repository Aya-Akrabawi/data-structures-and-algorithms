# Hashmap LEFT JOIN
 a function that LEFT JOINs two hashmaps into a single data structure.

## Challenge
 a function that LEFT JOINs two hashmaps into a single data structure.

## Solution
- a function that LEFT JOINs two hashmaps into a single data structure.
